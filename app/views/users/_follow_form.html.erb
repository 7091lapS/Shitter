<% unless current_user == @user %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'user_unfollow' %>
  <% else %>
    <%= render 'user_follow' %>
  <% end %>
  </div>
<% end %>
